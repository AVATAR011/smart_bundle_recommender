/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you later deploy API separately, keep API base in env:
  // NEXT_PUBLIC_API_BASE=https://api.yourdomain.com
};

export default nextConfig;
